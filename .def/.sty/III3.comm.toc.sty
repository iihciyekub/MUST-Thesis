\newcommand{\addtableofcontents}{
        \clearpage
        \ifwriteqEn
            \renewcommand{\contentsname}{CONTENT}
            \markboth{CONTENT}{}
            \tableofcontents

            % 如果設置 - 添加英文圖目錄
            \iffigt
                \clearpage
                % \renewcommand{\listfigurename}{圖目錄}
                \addcontentsline{toc}{chapter}{\hspace{\hdis em}List of Figures}
                \listoffigures
            \fi

            % 如果設置 - 添加中文表目錄
            \iftabt
                \clearpage
                % \renewcommand{\listtablename}{表目錄}
                \addcontentsline{toc}{chapter}{\hspace{\hdis em}List of Tables}
                \listoftables
            \fi
        \else
            % 添加目錄
            \renewcommand{\contentsname}{目錄}
            \markboth{目錄}{}
            \tableofcontents

            % 如果設置 - 添加中文圖目錄
            \iffigt
                \clearpage
                \renewcommand{\listfigurename}{圖目錄}
                \addcontentsline{toc}{chapter}{\hspace{\hdis em}圖目錄}
                \listoffigures
            \fi

            % 如果設置 - 添加中文表目錄
            \iftabt
                \clearpage
                \renewcommand{\listtablename}{表目錄}
                \addcontentsline{toc}{chapter}{\hspace{\hdis em}表目錄}
                \listoftables
            \fi
        \fi
        % 正文
        \clearpage
        \pagestyle{fancy}
        \pagenumbering{arabic}
        \setcounter{page}{1}
}
