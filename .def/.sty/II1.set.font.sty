% 导入 xeCJK 宏包，编译方式设定为 xelatex
\RequirePackage[T1]{fontenc}
\RequirePackage{fontspec}
\setmainfont{Times New Roman}


\RequirePackage{xeCJK}
% 设置中文主要字体为 KaiTi，并指定字体文件路径，同时设置自动伪斜体和伪粗体
\setCJKmainfont[
    Path = \resourcePath,
    AutoFakeSlant = 0.2,
    AutoFakeBold = 2.17
]{KaiTi}

% % 设置中文等宽字体为 KaiTi，并指定字体文件路径，同时设置自动伪斜体和伪粗体
% \setCJKmonofont[
%     Path = .def/,
%     AutoFakeSlant = 0.2,
%     AutoFakeBold = 2.17
% ]{KaiTi}

% % 设置中文无衬线字体为 KaiTi，并指定字体文件路径，同时设置自动伪斜体和伪粗体
% \setCJKsansfont[
%     Path = .def/,
%     AutoFakeSlant = 0.2,
%     AutoFakeBold = 2.17
% ]{KaiTi}

% 设置英文主要字体为 Times New Roman，并指定字体文件路径，同时设置自动伪斜体和伪粗体
% \setmainfont[
%     Path = .def/,
%     AutoFakeSlant = 0.42,
%     AutoFakeBold = 2.17
% ]{Times New Roman}

% \CJKsetecglue{\hskip 0.2em plus 0.1em minus 0.05em}

