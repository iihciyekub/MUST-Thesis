\RequirePackage{graphicx}
\RequirePackage{graphbox}
\RequirePackage{wrapfig}

% mwe 宏包提供了一些方便的命令和环境，用于插入示例图像、占位符文本等。以下是一些常用的命令和环境：
% \blindtext：插入一段占位符文本，用于测试布局。
% \blinddocument：生成一个包含多个段落和章节的占位符文档。
% \includegraphics：插入示例图像。mwe 宏包附带了一些示例图像，你可以使用这些图像进行测试。
% 例如，\includegraphics{example-image-a} 将插入一个示例图像 A。
\RequirePackage{mwe}