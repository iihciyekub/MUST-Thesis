\RequirePackage{titletoc}
\setcounter{tocdepth}{1}

\ifwriteqEn
    \titlecontents{chapter}
        [8.4 em] %\bfseries
        {\fontsize{14 pt}{24.1379310344828 pt}\selectfont}
        {\contentslabel[\bfseries Chapter \thecontentslabel]{7em}}
        {}
        {\titlerule*[0.5pc]{$\cdot$}\contentspage}
        []
    \titlecontents{section}
        [4 em]
        {\fontsize{14 pt}{24.1379310344828 pt}\selectfont  }
        {\contentslabel[\thecontentslabel]{2 em}}
        {}
        {\titlerule*[0.5pc]{$\cdot$}\contentspage}
        []
\else
    \titlecontents{chapter}
        [3.4 em] %\bfseries
        {\fontsize{14pt}{\baselineskip}\selectfont}
        {\contentslabel[第\thecontentslabel 章]{3.5 em}}
        {}
        { \titlerule*[0.5pc]{$\cdot$}\contentspage}
        []
    \titlecontents{section}
        [4.8 em]
        {\fontsize{14 pt}{\baselineskip}\selectfont}
        {\contentslabel[第\thecontentslabel 節]{3.5 em}}
        {}
        {\titlerule*[0.5pc]{$\cdot$}\contentspage}
        []
\fi



\ifwriteqEn
    % 修改圖目錄樣式
    \titlecontents{figure} % 修改圖目錄條目格式
      [0pt] % 左邊距
      {\addvspace{1em}} % 前間距
      {Fig \thecontentslabel\hspace{1em}} % 標籤格式
      {} % 無編號的情況
      {\titlerule*[8pt]{.}\contentspage} % 填充符和頁碼
\else
    % 修改圖目錄樣式
    \titlecontents{figure} % 修改圖目錄條目格式
      [0pt] % 左邊距
      {\addvspace{1em}} % 前間距
      {圖\thecontentslabel\hspace{1em}} % 標籤格式
      {} % 無編號的情況
      {\titlerule*[8pt]{.}\contentspage} % 填充符和頁碼
\fi



\ifwriteqEn
    % 修改表目录样式
    \titlecontents{table} % 修改表目录条目格式
      [0pt] % 左边距
      {\addvspace{1em}} % 前间距
      {Table \thecontentslabel\hspace{1em}} % 标签格式
      {} % 无编号的情况
      {\titlerule*[8pt]{.}\contentspage} % 填充符和页码
\else
    % 修改表目录样式
    \titlecontents{table} % 修改表目录条目格式
      [0pt] % 左边距
      {\addvspace{1em}} % 前间距
      {表\thecontentslabel\hspace{1em}} % 标签格式
      {} % 无编号的情况
      {\titlerule*[8pt]{.}\contentspage} % 填充符和页码
\fi


