\RequirePackage{array,tabularx, multirow,boldline,booktabs,longtable,makecell}

% 增加表脚注的宏包
\RequirePackage{threeparttable}


%读取csv的表宏包
\RequirePackage{csvsimple}

\RequirePackage[figuresright]{rotating}


% boxed：该选项在算法周围添加一个方框，将其与周围的文本区分开。
% ruled：该选项在算法的顶部和底部添加水平线（规则），强调算法的边界。
% shortend：该选项在算法的开头和结尾使用更短的线（垂直线）来标记算法的开始和结束。
% noline：该选项取消算法中行之间的垂直线。
% linesnumbered：该选项在算法的每行前面显示行号。
% commentsnumbered：该选项使算法中的注释也被编号。
\RequirePackage[boxed,
    ruled,
    shortend,
    noline,
    linesnumbered,
    commentsnumbered
]{algorithm2e}